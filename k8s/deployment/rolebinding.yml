apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: pod-permissions-binding
  namespace: challenges2  # Replace with the target namespace or remove if using ClusterRoleBinding
roleRef:
  kind: ClusterRole  # Use 'ClusterRole' for ClusterRoleBinding, 'Role' for RoleBinding
  name: pod-reader
  apiGroup: rbac.authorization.k8s.io
subjects:
  - kind: User  # Can be User, Group, or ServiceAccount
    name: clusterUser_challengeRg_challengeCluster  # Replace with the username, group, or service account name
    apiGroup: rbac.authorization.k8s.io
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: pod-writer-binding
  namespace: challenges2  # Replace with the target namespace or remove if using ClusterRoleBinding
roleRef:
  kind: ClusterRole  # Use 'ClusterRole' for ClusterRoleBinding, 'Role' for RoleBinding
  name: pod-writer
  apiGroup: rbac.authorization.k8s.io
subjects:
  - kind: User  # Can be User, Group, or ServiceAccount
    name: clusterUser_challengeRg_challengeCluster  # Replace with the username, group, or service account name
    apiGroup: rbac.authorization.k8s.io
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: pod-executor-binding
  namespace: challenges2  # Replace with the target namespace or remove if using ClusterRoleBinding
roleRef:
  kind: ClusterRole  # Use 'ClusterRole' for ClusterRoleBinding, 'Role' for RoleBinding
  name: pod-executor
  apiGroup: rbac.authorization.k8s.io
subjects:
  - kind: User  # Can be User, Group, or ServiceAccount
    name: clusterUser_challengeRg_challengeCluster  # Replace with the username, group, or service account name
    apiGroup: rbac.authorization.k8s.io
